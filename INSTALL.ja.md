# 導入・使い方

## Chrome ウェブストアから入れる
<https://chromewebstore.google.com/detail/manual-video-zoom/bangdldkomlemcendmmfecogcpjhoelf> を開き、「Chrome に追加」を押します。通常はこちらをお使いください。更新は Chrome が自動で行います。

## ソースを直接読み込む
1. ZIPを使う場合は先に展開し、`manual-video-zoom` フォルダーを移動しない場所へ置きます。
2. Chrome のアドレス欄に `chrome://extensions` と入力します。
3. 右上の「デベロッパー モード」をオンにします。
4. 「パッケージ化されていない拡張機能を読み込む」で、`manifest.json` が直接入っているフォルダーを選びます。
5. 拡張機能メニューから Manual Video Zoom をピン留めします。
6. [対象サイト](docs/sites.md)のページを再読み込みし、動画を再生します。

## 一覧にないサイトで使う
そのサイトを開いたままポップアップを開くと「このページではまだ有効になっていません」と表示され、サイト名入りの許可ボタンが出ます。押すとChromeの確認ダイアログが開き、許可するとその場で使えるようになります。以後は再読み込みやブラウザ再起動の後も自動で適用されます。

すべてのサイトで使いたい場合は、ポップアップ下部の「サイトへのアクセス」を開いて「すべてのサイトを許可する」を押してください。

許可の確認と取り消しは `chrome://extensions` の Manual Video Zoom の「詳細」→「サイトへのアクセス」で行えます。`chrome://` や `file://` など、拡張機能自体が入れないページでは許可ボタンは出ません。

## 黒帯を消す
**まず動画を全画面にしてから**拡張のアイコンを押し、「枠いっぱいに合わせる」を押してください。動画のサイズから必要な倍率を計算して一発で適用します。ウィンドウ表示のままだとすでに映像が枠いっぱいなため、このボタンは押せません。

自分で調整する場合は、数字を直接入力する、スライダーを動かす（ドラッグ中に映像へ即反映されます）、100／125／150／200% のプリセットを押す、−／+ を押す（1%ずつ、長押しで連続）のいずれかでどうぞ。

### 既存版から更新する
新版のファイルを元の読み込みフォルダーへ上書きし、`chrome://extensions` で Manual Video Zoom の更新（再読み込み）ボタンを押します。その後、動画ページも再読み込みしてください。追加したサイトへのアクセス許可をChromeが求めた場合は内容を確認してください。倍率設定は同じ拡張として更新すれば保持されます。

YouTubeでは通常動画のほか、同じサイトのShorts・広告・プレビューにも保存倍率が適用されます。必要に応じてリセットしてください。他サイト内のYouTube埋め込みプレイヤーは対象外です。

| 操作 | ショートカット |
|---|---|
| 1%拡大 | Alt + Shift + U |
| 1%縮小 | Alt + Shift + J |
| 100%に戻す | Alt + Shift + K |

キーが効かない場合は、ポップアップ下部の「キーボードショートカット」を開いて「ショートカットキーを変更」を押すか、`chrome://extensions/shortcuts` で割り当てを確認・変更してください。OSや他の拡張と競合する場合があります。初期値100%、上限400%です。

既に読み込んだ拡張では、ファイルの更新だけで既存のキー割り当てが変わらない場合があります。`chrome://extensions/shortcuts` を開き、Manual Video Zoom の各操作の鉛筆マークを押して、希望するキーを実際に押してください。ここから好きな組み合わせに変更できます。

倍率はサイトのオリジン（例：`https://www.netflix.com`）別に、この端末へ保存されます。次の再生や再読み込み時にも適用されます。別タブで操作した倍率は既存タブへ即時同期しません。

## 権限と対応範囲
対象サイトとURLの範囲は [サイト一覧](docs/sites.md) を参照してください。サイト内の動画を操作するため、Chrome が「サイト上のデータの読み取りと変更」を表示することがあります。ページへのアクセス権は実際にありますが、この実装は動画要素の寸法とスタイルを扱い、外部通信やアカウント情報の収集は行いません。権限を減らす場合は拡張の「詳細」→サイトへのアクセスで制限できます。

導入時に許可されるのは上記の対象サイトだけで、それ以外は利用者がポップアップから許可した分だけです。字幕が映像に焼き込まれている場合、字幕も端と一緒に切れます。別要素の字幕・操作ボタンは拡大しません。映像の縦横比を保つため、黒帯をなくすと映像の一部が画面外へ切れます。

## うまく動かない場合
- 「動画が見つかりません」：動画を再生し、1秒ほど待って操作します。埋め込みフレームや特殊なプレイヤー構造は未対応です。
- 「枠いっぱいに合わせる」が押せない：全画面にしていないか、動画のサイズがまだ読み込まれていません。再生を開始し、全画面にしてからポップアップを開き直してください。
- 「このページではまだ有効になっていません」：表示された許可ボタンを押します。対象サイトなのに出る場合は、導入・更新直後でページが古いためなので、再読み込みするかそのボタンを押してください。
- 全画面だけ効かない／字幕が切れる：一度リセットし、全画面を解除して確認します。実サービスのプレイヤー次第で調整が必要です。
- 設定を保存できない：拡張を再読み込みし、サイトも再読み込みして再操作します。
- 元に戻す：リセットを押します。問題が残れば拡張を無効にしてページを再読み込みします。
- 削除：`chrome://extensions` の「削除」。設定も削除されます。開いている動画ページは再読み込みしてください。

各対象サイトの再生、DRM、全画面の実機確認は未実施です。自動テストの結果と手動確認項目は [verification.md](docs/verification.md) に記載しています。

## 表示と言語
ポップアップの配色はブラウザのライト／ダーク設定に合わせて切り替わります。Chromeの表示言語が日本語なら日本語、それ以外は英語で表示します。同じ拡張に両言語が含まれています。ポップアップにはChromeで現在割り当てられているショートカットを表示します。
